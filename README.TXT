Hi,
-you need to install xamp
-and php composer
-then extract the "Generating lar" file
-inside the folder( "Generating la ") open cmd
-type "php artisan serve"
-open your browser 
-type "http://localhost:8000/" to your url
